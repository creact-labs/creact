{
  "name": "@creact-labs/creact-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/creact",
    "test": "npm test -w packages/creact",
    "typecheck": "npm run typecheck -w packages/creact",
    "coverage:all": "npm run test:coverage -w packages/creact && vitest --run --coverage --root packages/testing && vitest --run --coverage --root packages/create-creact-app && npm run coverage:merge",
    "coverage:merge": "node scripts/merge-coverage.mjs coverage/coverage-final.json packages/creact/coverage/coverage-final.json packages/testing/coverage/coverage-final.json packages/create-creact-app/coverage/coverage-final.json",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "author": "CReact Labs",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/creact-labs/creact.git"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prettier": "^3.6.2",
    "typescript": "^5.0.0"
  }
}