{
  "name": "@creact-labs/testing",
  "version": "0.1.1",
  "type": "module",
  "description": "Testing utilities for CReact applications",
  "files": ["dist"],
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b && tsc-alias -p tsconfig.json --resolve-full-paths && tsc-alias -p ../creact/tsconfig.json --resolve-full-paths",
    "test": "vitest --run",
    "test:coverage": "vitest --run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "author": "Daniel Coutinho Ribeiro <dcoutinho.96@gmail.com>",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/creact-labs/creact.git",
    "directory": "packages/testing"
  },
  "peerDependencies": {
    "@creact-labs/creact": "^0.4.0"
  },
  "devDependencies": {
    "@creact-labs/creact": "*",
    "tsc-alias": "^1.9.1",
    "vitest": "^3.2.4"
  }
}
