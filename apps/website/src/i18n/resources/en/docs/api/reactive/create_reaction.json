{
  "title": "createReaction",
  "description": "Separates tracking from execution. Track one expression, run a different function when it changes. The reaction fires once, then must be re-armed.",
  "signature": "createReaction(onInvalidate: () => void, options?: EffectOptions): (tracking: () => void) => void",
  "param_on_invalidate_name": "<0>onInvalidate</0>",
  "param_on_invalidate_type": "<0>() => void</0>",
  "param_on_invalidate_desc": "Callback that fires once when tracked signals change.",
  "param_options_name": "<0>options</0>",
  "param_options_type": "<0>EffectOptions</0>",
  "param_options_desc": "Optional. <0>name</0> for debugging.",
  "returns_body": "A function <0>(tracking: () => void) => void</0> that arms the reaction. Call it with a tracking expression. Signals read inside are tracked. When any of them change, <1>onInvalidate</1> fires once and tracking stops until you call the function again."
}
