{
  "title": "createReaction",
  "description": "Separates tracking from execution. Track one expression, run a different function when it changes. The reaction fires once, then must be re-armed.",
  "signature": "createReaction(onInvalidate: () => void, options?: EffectOptions): (tracking: () => void) => void",
  "param_on_invalidate_name": "<code>onInvalidate</code>",
  "param_on_invalidate_type": "<code>() =&gt; void</code>",
  "param_on_invalidate_desc": "Callback that fires once when tracked signals change.",
  "param_options_name": "<code>options</code>",
  "param_options_type": "<code>EffectOptions</code>",
  "param_options_desc": "Optional. <code>name</code> for debugging.",
  "returns_body": "A function <code>(tracking: () =&gt; void) =&gt; void</code> that arms the reaction. Call it with a tracking expression. Signals read inside are tracked. When any of them change, <code>onInvalidate</code> fires once and tracking stops until you call the function again."
}
