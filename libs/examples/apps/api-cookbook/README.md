# api-cookbook

A cookbook of every CReact core API. Each module under `src/` backs one
docs API page: the website slices `#region` blocks from these files, so
every published sample compiles against the real library.

## Run

```bash
npm install
npx creact index.tsx
```

The entry exercises a slice of the cookbook; the modules are the point —
open them alongside the API reference.
