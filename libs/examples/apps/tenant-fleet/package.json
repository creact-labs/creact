{
  "name": "@creact-labs/example-tenant-fleet",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "A multi-tenant provisioner where every tenant runs as its own sovereign runtime",
  "scripts": {
    "start": "creact --watch index.tsx",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@creact-labs/creact": "*",
    "@creact-labs/example-file-memory": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
