# Port the HTTP channel listens on
PORT=3000
# Anthropic API key for the AI generation recipe
ANTHROPIC_API_KEY=
# AWS region and credentials for the S3 publishing recipe
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Deployment environment picked up by the environment-variables recipe
NODE_ENV=development
