# Port the generation channel listens on
PORT=3000
# Anthropic API key for HTML generation (when wired to the real SDK)
ANTHROPIC_API_KEY=
# AWS region and credentials for site deployment (when wired to the real SDK)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
