{
  "name": "@creact-labs/example-uptime-monitor",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Pings real URLs, tracks status transitions, and publishes a static status page",
  "scripts": {
    "start": "creact --watch index.tsx",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@creact-labs/creact": "*",
    "@creact-labs/example-file-memory": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
