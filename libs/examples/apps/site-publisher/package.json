{
  "name": "@creact-labs/example-site-publisher",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Publishes a local folder to an S3 static site, diff-aware, with the real AWS SDK",
  "scripts": {
    "start": "creact index.tsx",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@creact-labs/creact": "*",
    "@creact-labs/example-file-memory": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
