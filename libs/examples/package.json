{
  "name": "@creact-labs/examples",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Real, type-checked CReact example apps — the website imports source regions from them for its code samples",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@creact-labs/creact": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
